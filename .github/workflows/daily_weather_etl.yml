name: Daily Weather ETL

on:
  schedule:
    # Runs daily at 11:10 UTC (~6:10 AM Toronto in winter)
    - cron: "10 11 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-etl:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pipeline
        run: |
          python -m etl.run_weather_pipeline

      - name: Commit and push updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/raw warehouse reports README.md
          git commit -m "Daily update (weather ETL)" || echo "No changes to commit"
          git push
